@model CinemaFinalMVC.Models.CelebrityProfession

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>CelebrityProfession</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CelebrityProfessionId" />
            <div class="form-group">
                <label asp-for="CelebrityId" class="control-label"></label>
                <select asp-for="CelebrityId" class="form-control" asp-items="ViewBag.CelebrityId"></select>
                <span asp-validation-for="CelebrityId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ProfessionId" class="control-label"></label>
                <select asp-for="ProfessionId" class="form-control" asp-items="ViewBag.ProfessionId"></select>
                <span asp-validation-for="ProfessionId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
